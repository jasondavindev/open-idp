apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: grafana
  namespace: grafana
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`k8s.com`) && PathPrefix(`/grafana`)
      services:
        - kind: Service
          name: grafana
          port: 80
